#!/bin/sh
# Optional environment overrides for dotfiles scripts (variables only).

export EDITOR=nvim
export VISUAL=code

export DOTFILES_OS_ARCH_ASSUME_YES=1
export DOTFILES_PROMPT_IDENTITY=always

# Disable LightPanda telemetry
export LIGHTPANDA_DISABLE_TELEMETRY=true
